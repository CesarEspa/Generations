const {mean} = require('./funciones.js');

/*
test('sumar(2,5) debe retornar (7)', () => {
    expect(sumar(2, 5)).toBe(7);
});


test('restar 5 - 2 es igual a 3', () => {
    expect(restar(5, 2)).toBe(3);
});
*/

test('mean(20,10) deberia ser (15))', () => {
    expect(mean(10,20,30)).toBe(20);

})

